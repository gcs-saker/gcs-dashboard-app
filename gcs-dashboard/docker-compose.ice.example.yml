services:
  mediamtx:
    environment:
      MTX_WEBRTCICESERVERS2_0_URL: "${WEBRTC_STUN_URL:-stun:stun.l.google.com:19302}"
      MTX_WEBRTCICESERVERS2_1_URL: "${WEBRTC_TURN_URL:-turn:turn:3478?transport=udp}"
      MTX_WEBRTCICESERVERS2_1_USERNAME: "${WEBRTC_TURN_USERNAME:-gcs-turn}"
      MTX_WEBRTCICESERVERS2_1_PASSWORD: "${WEBRTC_TURN_PASSWORD:-replace-with-secret-outside-git}"
      MTX_WEBRTCICESERVERS2_1_CLIENTONLY: "${WEBRTC_TURN_CLIENT_ONLY:-false}"
