# Backend standalone env example.
# Copy to backend/.env for local Docker Compose or set equivalent variables in staging/prod secrets.

# Do not commit real production credentials.
DATABASE_URL=postgresql+psycopg2://gcs_geo:change-me-local-postgres@postgres:5432/gcs_geo

# JWT signing secret for dashboard/operator API access.
# Use a generated secret in each environment and never commit real values.
AUTH_JWT_SECRET=change-me-to-a-random-32-plus-character-secret
AUTH_ACCESS_TOKEN_EXPIRE_MINUTES=30
AUTH_REFRESH_TOKEN_EXPIRE_MINUTES=10080
AUTH_REFRESH_COOKIE_SECURE=false
AUTH_REFRESH_COOKIE_SAMESITE=lax
AUTH_JWT_ISSUER=gcs-saker
BACKEND_CORS_ALLOW_ORIGINS=http://localhost:5173,http://localhost:5174

MQTT_HOST=mqtt
MQTT_PORT=1883
MQTT_CLIENT_ID=gcs_backend_pub
MQTT_KEEPALIVE=60
MQTT_RECONNECT_MIN_DELAY_SECONDS=1
MQTT_RECONNECT_MAX_DELAY_SECONDS=30
MQTT_MAX_INFLIGHT_MESSAGES=20
# Set these only when the MQTT broker requires authentication.
# Do not expose them to frontend build-time env.
MQTT_USERNAME=gcs_backend_pub
MQTT_PASSWORD=change-me-mqtt-password
CONTROL_MESSAGE_SENDER=mqtt
# Keep disabled unless the MQTT v2 telemetry consumer should run inside this process.
MQTT_V2_TELEMETRY_SUBSCRIBER_ENABLED=false
TELEMETRY_BUFFER_AUTO_FLUSH_MAX_ITEMS=1000

# Public MediaMTX playback base URLs used by the backend Playback URL Builder.
# WHEP endpoint: {MEDIAMTX_PUBLIC_WEBRTC_BASE_URL}/{streamPath}/whep
# HLS playlist endpoint: {MEDIAMTX_PUBLIC_HLS_BASE_URL}/{streamPath}/index.m3u8
MEDIAMTX_PUBLIC_WEBRTC_BASE_URL=http://localhost:8889
MEDIAMTX_PUBLIC_HLS_BASE_URL=http://localhost:8888

# Browser ICE server list returned by /api/v1/streams/ice-servers.
# Keep real TURN credentials in environment secrets only.
WEBRTC_STUN_URL=stun:stun.l.google.com:19302
# WEBRTC_TURN_URL=turn:a4ai.tplinkdns.com:3478?transport=udp
# WEBRTC_TURN_USERNAME=gcs-turn
# WEBRTC_TURN_PASSWORD=replace-with-secret-outside-git

# Dashboard map provider config returned by /api/v1/map/config.
# Default satellite provider does not require a dashboard-side API key.
DASHBOARD_MAP_PROVIDER=esri-satellite
DASHBOARD_MAP_STYLE_URL=https://services.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}
DASHBOARD_MAP_ATTRIBUTION=Esri World Imagery
DASHBOARD_MAP_REQUIRES_API_KEY=false
