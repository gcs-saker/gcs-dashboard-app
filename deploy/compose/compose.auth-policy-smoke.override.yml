services:
  auth-policy:
    ports:
      - "${AUTH_POLICY_SMOKE_BIND_ADDR:-127.0.0.1}:${AUTH_POLICY_SMOKE_PORT:-28080}:8080"
  media-control:
    ports:
      - "${MEDIA_CONTROL_SMOKE_BIND_ADDR:-127.0.0.1}:${MEDIA_CONTROL_SMOKE_PORT:-28081}:8081"
