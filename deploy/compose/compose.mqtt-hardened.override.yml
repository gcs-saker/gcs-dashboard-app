# Deprecated compatibility override.
#
# Hardened MQTT is now the default in compose.single-node.poc.yml.
# Keep this file as a no-op so older smoke/run commands that still append it do
# not reintroduce duplicate Mosquitto config/password bind mounts.

services:
  mqtt:
    labels:
      kr.co.a4ai.gcssaker.mqtt.hardened: "default"
