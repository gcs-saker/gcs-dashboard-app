# GCS-Saker v1.0.0 candidate

Status: `BLOCKED_NOT_TAGGED`

This candidate binds the current software baseline, security gates, deployment automation, and
delivery documents. It is not a release and no `v1.0.0` tag may be created until every blocking
item in `delivery-readiness.yml` has objective evidence.

Implemented scope includes server-owned stream/session/group routing, WebRTC playback and Talkback,
MQTT/gRPC control boundaries, administrator and group policy, trusted public TLS, short-lived TURN
credentials, internal mTLS profiles, MFA, signed-release enforcement, STIG/RMF assessment, fuzz/DAST
harnesses, audit-chain recovery verification, and software voice qualification.

Known blockers are unresolved license dispositions, Server-01 MFA/PKI deployment, independent/WORM
audit storage, 16-stream 30-minute load evidence, destructive recovery rehearsal, independent
penetration testing, and physical voice intelligibility evidence.
